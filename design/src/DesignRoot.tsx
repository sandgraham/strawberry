import React from "react"
import "./index.css"

/**
 * All this does is import our shared global index.css file. Perhaps in the
 * future it will do more!
 */
export function DesignRoot ({ children }: { children: any }) {
  return children;
}
